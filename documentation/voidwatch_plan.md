# Voidwatch Implementation Assessment and Completion Plan

## Source baseline

This assessment is based on the following retail reference pages:

- BG Wiki Voidwatch Guide: <https://www.bg-wiki.com/ffxi/Voidwatch_Guide>
- BG Wiki Voidwatch category: <https://www.bg-wiki.com/ffxi/Category:Voidwatch>
- BG Wiki Voidwatch Quests category: <https://www.bg-wiki.com/ffxi/Category:Voidwatch_Quests>
- BG Wiki Provenance page: <https://www.bg-wiki.com/ffxi/Provenance>

The key retail requirements from those sources are:

1. Entry requirements: level 75+, Adventurer's Certificate, and at least one stratum abyssite.
2. Starting-city officers grant the initial crimson, indigo, and jade stratum abyssites; other paths grant white, ashen, hyacinth, and amber abyssites through the Voidwatch questline or regional starters.
3. Voidstones are generated by Voidwatch Officers on a 20-hour base timer, reduced by periapts, and may also be obtained by trading Voiddust.
4. Planar Rifts initiate battles, optionally accept ascent items and phase-displacer clusters, validate the initiator's abyssite tier, and spawn the correct Voidwatch NM.
5. Each participating player may spend a voidstone for individual post-battle rewards from a Riftworn Pyxis; without a stone, participation still allows base rewards and progression credit where appropriate.
6. Spectral alignment has red, blue, green, yellow, and white components. Staggers, synchronic blitz, ascent items, Treasure Hunter, periapts, and Voidwatch campaigns influence final values.
7. Periapts and atmacites provide temporary items, voidstone capacity/timer bonuses, alignment bonuses, and in-battle player augment effects.
8. Stratum abyssites upgrade tier-by-tier after all required NMs in the current tier are defeated and the player asks an Atmacite Refiner for examination.
9. Atmacite Refiners provide atmacite upgrade/equip services and Voidwatch teleportation to battle areas the player can initiate, for cruor.
10. Provenance requires late Jeuno-line Voidwatch progress, uses Kupofried's corundum and petrifacts instead of ordinary voidstones for final access, contains the three Radiance fights and Provenance Watcher, and is entered through Walk of Echoes.
11. Voidwatch has a 30-quest storyline/category footprint, including the city path, Jeuno path, Zilart, Tavnazia, Aht Urhgan, Provenance, and finale quests.

## Current codebase inventory (May 9, 2026)

The repository contains a significant amount of static data for Voidwatch:

- `settings/default/main.lua` already exposes `ENABLE_VOIDWATCH` in the main settings table.
- `sql/npc_list.sql` contains content-tagged Voidwatch NPCs and objects, including 202 Planar Rifts, 201 Riftworn Pyxides, 19 Voidwatch Purveyors, 17 Atmacite Refiners, and 12 Voidwatch Officers.
- `sql/char_points.sql` stores `voidstones` and `kupofried_corundums` point/currency fields that can back the officer and Provenance systems.
- `sql/status_effects.sql` defines the hidden `voidwatcher` effect.
- `sql/mob_pools.sql`, `sql/mob_groups.sql`, and `sql/mob_spawn_points.sql` already include many named Voidwatch NMs, including the starter-nation NMs, Jeuno/Zilart/Tavnazia/Aht Urhgan targets, Radiance encounters, and Provenance Watcher.
- `sql/zone_settings.sql` defines the Provenance zone.
- `src/map/utils/charutils.cpp` includes `VOIDWATCH` in the content-tag filtering list used when content restriction is enabled.

However, the implementation is mostly static data. The repository currently lacks Lua scripts or C++ systems that make the listed NPCs, rifts, pyxides, quests, rewards, alignment, and progression function as retail Voidwatch.

## Current codebase inventory (May 30, 2026)

The repository now has both substantial static data and an initial Lua implementation slice for Voidwatch.

Static/data support already present:

- `settings/default/main.lua` exposes `ENABLE_VOIDWATCH` in the main settings table.
- `sql/npc_list.sql` contains content-tagged Voidwatch NPCs and objects, including Planar Rifts, Riftworn Pyxides, Voidwatch Purveyors, Atmacite Refiners, and Voidwatch Officers.
- `sql/char_points.sql` stores `voidstones` and `kupofried_corundums` point/currency fields that can back the officer and Provenance systems.
- `sql/status_effects.sql` defines the hidden `voidwatcher` effect.
- `sql/mob_pools.sql`, `sql/mob_groups.sql`, and `sql/mob_spawn_points.sql` include many named Voidwatch NMs, including starter-nation NMs, Jeuno/Zilart/Tavnazia/Aht Urhgan targets, Radiance encounters, and Provenance Watcher.

Scripted support already present:

- `scripts/globals/voidwatch/voidwatch.lua` is the active shared module location. Do not add a competing root-level `scripts/globals/voidwatch.lua` module unless the codebase is intentionally refactored.
- The module has an `xi.settings.main.ENABLE_VOIDWATCH` runtime guard, level/certificate base requirement checks, route names, stratum mappings, abyssite key-item tables, selected periapt lists, atmacite lists, cells, Voiddust, phase displacers, petrifacts, corundums, and route/tier/NM data.
- The module has helpers for route lookup, maximum route tier, player abyssite tier, tier initiation checks, per-NM completion variables, tier-complete checks, abyssite-upgrade eligibility, starter route detection, and starter abyssite lookup/granting.
- The module has voidstone helpers for carried key-item count, next key item, capacity, timer interval, stored stock, 20-hour accrual, periapt capacity/timer modifiers, withdrawing stored stones to key items, one-pouch Voiddust trades, and voidstone spending for initiated rift battles.
- The module has starter-rift slices for San d'Oria tier 1 Sarimanok rifts in East Ronfaure, Bastok tier 1 Sallow Seymour rifts in North Gustaberg, and Windurst tier 1 Virvatuli rifts in West Sarutabaruta, including requirement validation, NM spawn state, initiator completion credit, and placeholder pyxis reward eligibility.
- Three starter-city `Voidwatch_Officer` scripts exist in Southern San d'Oria, Bastok Markets, and Windurst Walls. They share the module for trigger/trade handling and route-specific starter abyssite grants. Three starter-city `Atmacite_Refiner` scripts share the module for abyssite examination/upgrades and limited starter-route teleport validation/cost handling. Three starter-city `Voidwatch_Purveyor` scripts share the module for limited cell, Voiddust, and phase-displacer purchases. Full atmacite services are not yet implemented.
- `scripts/tests/systems/voidwatch.lua` covers toggle behavior, base requirements, route table consistency, starter abyssite mapping and idempotency, disabled/missing-requirement cases, periapt capacity/timer helpers, stock accrual, voidstone withdrawal, Voiddust trade behavior, starter-refiner abyssite examination outcomes, starter-route refiner teleport validation/cost handling, starter-purveyor stock/purchase validation, starter nation rift initiation/completion/pyxis slices, Bastok Sallow Seymour starter-rift table/progression checks, and Windurst Virvatuli jade-abyssite validation.

## Completed implementation slices

### Phase 1: Toggle, data model, and shared module foundation (complete for current needs)

Completed enough to support implementation PRs:

1. `ENABLE_VOIDWATCH` runtime guard through `xi.voidwatch.isEnabled()`.
2. Constants/tables for current route, abyssite, periapt, atmacite, cell, Voiddust, petrifact, and corundum use.
3. BG Wiki-derived route/tier/NM inventory for starter cities, Jeuno, Zilart, Tavnazia, Aht Urhgan, and Provenance.
4. Helpers for requirements, current abyssite tier, route/tier lookup, completed NM flags, and upgrade eligibility.
5. Unit coverage for core table integrity and toggle behavior.

Remaining Phase 1-style additions should be folded into implementation PRs only when consumed by real scripts, such as rift-to-NM IDs, pyxis state, reward/alignment structures, temporary-item helpers, and additional retail service constants.

### Phase 2A: Starter-city officers and initial abyssites (complete)

Completed:

1. Southern San d'Oria, Bastok Markets, and Windurst Walls officer scripts call shared Voidwatch behavior.
2. Officer triggers guard on `ENABLE_VOIDWATCH`, level 75+, and Adventurer's Certificate.
3. Starter officers grant the expected first-tier stratum abyssites: crimson for San d'Oria, indigo for Bastok, and jade for Windurst.
4. Starter grants are idempotent and covered by tests.

### Phase 2B: Basic voidstone storage/accrual and Voiddust trades (substantially complete)

Completed:

1. Stored voidstones use the `voidstones` char-point currency.
2. Accrual uses a persistent `[Voidwatch]NextVoidstone` char variable and a 20-hour base interval.
3. Frontiers periapts increase capacity, and exploration periapts reduce the timer interval.
4. Starter officers withdraw stored voidstones into carried voidstone key items when possible.
5. Trading exactly one pouch of Voiddust to a starter officer awards one carried voidstone when the player meets requirements and has capacity.
6. Coverage exists for capacity/timer calculations, accrual, withdrawal, and Voiddust trade handling.

### Phase 3A: Starter-city Atmacite Refiner abyssite examination (complete for starter routes)

Completed:

1. Southern San d'Oria, Bastok Markets, and Windurst Walls Atmacite Refiner scripts call shared Voidwatch behavior.
2. Refiner triggers guard on `ENABLE_VOIDWATCH`, level 75+, and Adventurer's Certificate.
3. The shared refiner slice examines the starter San d'Oria, Bastok, and Windurst routes for upgrade eligibility.
4. Eligible starter-route abyssites are replaced with the next-tier abyssite after all NMs in the current tier are marked complete.
5. Disabled-content, missing-requirement, no-abyssite, incomplete-tier, successful-upgrade, maximum-tier, and starter-route coverage cases are covered by tests.

### Phase 3B: Starter-city Atmacite Refiner teleport validation (complete for starter routes)

Completed:

1. The shared Voidwatch module defines explicit starter-route teleport destinations for San d'Oria, Bastok, and Windurst Voidwatch operation zones.
2. Destination availability is validated from the player's current starter-route abyssite tier and the standard `ENABLE_VOIDWATCH`, level 75+, and Adventurer's Certificate requirements.
3. Refiner teleports charge cruor only after destination validation succeeds and report insufficient-cruor failures without moving the player.
4. The initial teleport slice remains intentionally narrow and does not add atmacite equip/upgrade menus, Planar Rift spawning, pyxides, alignment, rewards, or quest cutscenes.
5. Tests cover destination table integrity, tier-based filtering, disabled/missing-requirement behavior, no-abyssite behavior, tier validation, insufficient-cruor handling, and successful cruor charging/teleport movement.

### Phase 3C: Starter-city Voidwatch Purveyor shops (complete for starter routes)

Completed:

1. Southern San d'Oria, Bastok Markets, and Windurst Walls purveyor scripts call shared Voidwatch behavior.
2. The shared module defines starter-purveyor stock for the four alignment cells, one-pouch Voiddust purchases, and phase displacers.
3. Purveyor access is guarded by `ENABLE_VOIDWATCH`, level 75+, and Adventurer's Certificate requirements.
4. Purchases validate the selected item, payment, and free inventory space before charging conquest points or gil.
5. This slice remains intentionally narrow and does not add atmacite equip/upgrade menus, Planar Rift spawning, pyxides, alignment, rewards, pulse-cell exchanges, or quest cutscenes.
6. Tests cover purveyor table integrity, disabled/missing-requirement behavior, invalid item selection, payment failures, inventory failures, and successful conquest-point/gil purchases.

### Phase 4A: One starter-route Planar Rift vertical slice (complete for San d'Oria tier 1)

Completed:

1. East Ronfaure `Planar_Rift` scripting initiates the San d'Oria tier-1 Sarimanok operation from the three tagged rift positions.
2. The shared module validates `ENABLE_VOIDWATCH`, level 75+, Adventurer's Certificate, crimson stratum abyssite tier, voidstone availability, and already-spawned NM state before initiation.
3. Initiation spends one carried voidstone, spawns the mapped Sarimanok mob, claims it to the initiator, and stores compact rift/initiator state on the NM.
4. Sarimanok death grants the existing per-NM completion variable to the initiator so starter Atmacite Refiner abyssite upgrades can observe real battle credit.
5. The slice intentionally excludes pyxis rewards, spectral alignment, phase-displacer/ascent-cell effects, weakness/stagger systems, broad route coverage, and quest cutscenes.
6. Tests cover rift table integrity, disabled/missing-requirement behavior, abyssite and voidstone validation, voidstone spending, NM spawn state, and completion-credit setting.

### Phase 4B: Starter pyxis placeholder and second starter-rift route (complete for current needs)

Completed:

1. East Ronfaure `Riftworn_Pyxis` scripting exposes a guarded placeholder interaction for the Sarimanok slice.
2. Sarimanok death records per-initiator pyxis reward eligibility, and inspecting the mapped pyxis consumes that placeholder eligibility while deferring full loot/alignment generation.
3. North Gustaberg `Planar_Rift`, `Riftworn_Pyxis`, and Sallow Seymour mob scripts reuse the shared starter-rift lifecycle for the Bastok tier-1 route.
4. The shared rift table maps the three North Gustaberg rifts to the three Sallow Seymour mob IDs and their paired pyxides, with indigo abyssite progression validation.
5. This slice intentionally excludes full pyxis rewards, spectral alignment, phase-displacer/ascent-cell effects, weakness/stagger systems, broad route coverage beyond the two starter slices, and quest cutscenes.
6. Tests cover expanded rift/pyxis table integrity, disabled/missing-requirement behavior, pyxis eligibility consumption, and Bastok-specific indigo abyssite validation.

### Phase 4C: Third starter-rift route (complete for current needs)

Completed:

1. West Sarutabaruta `Planar_Rift` and `Riftworn_Pyxis` scripts reuse the guarded starter-rift lifecycle for Windurst tier 1.
2. The shared rift table maps the three West Sarutabaruta rifts to the three Virvatuli mob IDs and their paired pyxides.
3. Virvatuli death uses the common Voidwatch NM death/despawn hooks to set Windurst completion credit and placeholder pyxis eligibility for the initiator.
4. Windurst initiation validates jade stratum abyssite progression independently from the San d'Oria crimson and Bastok indigo routes.
5. This slice intentionally excludes full pyxis rewards, spectral alignment, phase-displacer/ascent-cell effects, weakness/stagger systems, broader Windurst route coverage, and quest cutscenes.
6. Tests cover starter-rift table integrity across all three nations and Windurst-specific jade abyssite validation.

## Accuracy and completeness gaps

### Toggle and content gating

- The main toggle exists and the current Lua entry points guard on it.
- Voidwatch SQL rows use the `VOIDWATCH` content tag, so the existing `RESTRICT_CONTENT` mechanism can hide most static Voidwatch NPC/object data when `ENABLE_VOIDWATCH = 0`.
- Still needed: continue adding runtime guards to every future Voidwatch Lua entry point, because static NPC rows may still be loaded when `RESTRICT_CONTENT = 0`.

### NPCs and object interaction

- Present: shared module, the three starter-city `Voidwatch_Officer` scripts, the three starter-city `Atmacite_Refiner` scripts, the three starter-city `Voidwatch_Purveyor` scripts, East Ronfaure `Planar_Rift`/`Riftworn_Pyxis` scripts for the San d'Oria tier-1 Sarimanok slice, North Gustaberg `Planar_Rift`/`Riftworn_Pyxis` scripts for the Bastok tier-1 Sallow Seymour slice, and West Sarutabaruta `Planar_Rift`/`Riftworn_Pyxis` scripts for the Windurst tier-1 Virvatuli slice.
- Missing: scripts for non-starter officers, non-starter Atmacite Refiners, non-starter Voidwatch Purveyors, most Planar Rifts, Riftworn Pyxides, Provenance `Regal_Pawprints`, and Provenance/Walk of Echoes access objects.
- Missing: refiner behavior for non-starter abyssite upgrade checks, atmacite equip/upgrade, and full retail teleport menus.
- Missing: retail event/cutscene integration for officers and storyline NPCs. The current starter officer implementation is functional/system-message based, not a retail-accurate event flow.

### Progression and quests

- Present: generic per-NM completion variable helpers, tier-complete checks, abyssite-upgrade eligibility helpers, and battle completion credit for the Sarimanok, Sallow Seymour, and Virvatuli starter slices.
- Missing: battle code that sets completion flags for the remaining Voidwatch NMs.
- Missing: all 30 Voidwatch quest scripts listed by BG Wiki's Voidwatch Quests category.
- Missing: quest-region placement and NPC event handling for starter-city, Jeuno, Zilart, Tavnazia, Aht Urhgan, Provenance, and finale quests.
- Missing: key-item award logic for Voidwatcher's emblems, non-starter route abyssites, petrifacts, Kupofried's corundums, and final storyline flags.
- Missing: requirement links such as completing the three starter paths before Jeuno initiation and late Jeuno progress before Provenance access.

### Battles

- Present: narrow East Ronfaure Sarimanok and North Gustaberg Sallow Seymour Planar Rift initiation flows with initiator requirement checks, carried-voidstone spending, spawned-NM state, completion credit, and placeholder pyxis eligibility.
- Missing: full Planar Rift initiation flow, including party/alliance validation, rank/key-item checks beyond the starter slice, participation eligibility for non-initiators, and broad route/rift coverage.
- Missing: support for ascent item trades, phase displacer clusters, corundum spending, and battlefield reservation where applicable.
- Missing: full battle status state that associates a spawned NM with all eligible players and pyxis state.
- Missing: retail-style Voidwatch weaknesses, proc messages, stagger duration, temporary item restoration, and synchronic blitz.
- Missing: spectral alignment calculations and caps.
- Missing: per-player Riftworn Pyxis reward generation, including individual loot pools, alignment-influenced quality/quantity, key-item/atmacite/periapt acquisition, cruor/EXP awards, campaign overrides, and pyxis cleanup.
- Missing: many NM-specific mechanics, scripts, mods, and drop tables are likely incomplete even where spawn data exists.

### Provenance

- Missing: Walk of Echoes to Provenance access flow and return handling.
- Missing: Kupofried's corundum consumption for the three Radiance fights.
- Missing: Beguiling, Maddening, and Seductive petrifact reward flow.
- Missing: Provenance Watcher entry requiring the three petrifacts.
- Missing: Regal Pawprints atmacite/refiner behavior inside Provenance.
- Missing: Provenance-specific battlefield timers, member limits, titles, and final quest completion.

## Completion plan

### Phase 1: Toggle, data model, and shared module foundation

1. Keep `settings/default/main.lua` as the authoritative toggle location and update the comment to clarify that `ENABLE_VOIDWATCH` gates scripts while `RESTRICT_CONTENT` hides tagged SQL content.
2. Add `scripts/globals/voidwatch.lua` with:
   - `isEnabled()` guard using `xi.settings.main.ENABLE_VOIDWATCH`.
   - constants for stratum colors, tiers, route names, abyssite key items, periapts, atmacites, cells, Voiddust, phase displacers, petrifacts, and corundums.
   - route/tier/NM tables based on BG Wiki's Voidwatch Ops tables.
   - helpers for requirement checks, current abyssite tier, completed NM flags, and upgrade eligibility.
3. Add tests/specs for route table integrity and toggle behavior where practical.

### Phase 2: Officers, voidstones, and starter access

1. Implement shared `Voidwatch_Officer` scripts in all officer zones.
2. Enforce level 75+, Adventurer's Certificate, and enabled-content checks before issuing starter abyssites.
3. Implement voidstone accrual using a persistent timestamp and capacity derived from frontiers periapts.
4. Implement Voiddust trades for voidstone stock.
5. Add clear disabled-content messaging when the toggle is off.

## Recommended next PR

The next PR should move to **Phase 4D: broaden placeholder pyxis cleanup for starter rifts**. The current starter lifecycle now covers all three present-day starter-nation tier-1 rift paths, but placeholder pyxis state remains an indefinite per-player flag.

Recommended scope:

1. Add a narrow cleanup mechanism for placeholder starter pyxis eligibility, such as timestamped eligibility, timeout on inspection, or cleanup when a mapped rift is reinitiated.
2. Keep the same runtime validation pattern: `ENABLE_VOIDWATCH`, level 75+, Adventurer's Certificate, and mapped starter pyxis/rift validation.
3. Reuse the shared pyxis helpers and avoid route-specific duplicate logic.
4. Add focused tests for eligibility setting, timeout or cleanup behavior, invalid pyxis IDs, disabled/missing-requirement behavior, and eligibility consumption.
5. Keep the PR intentionally narrow: do not add full pyxis rewards, spectral alignment, weakness/stagger systems, campaign modifiers, broad route coverage beyond cleanup, or quest cutscenes.
6. Update scripts/tests/systems/voidwatch.lua to treat San d'Oria's branch the same as Bastok & Windurst; crimson_stratum_abyssite is currently required on every check, which is wrong, and it should only be checked on the San d'Oria branch.

## Remaining completion plan

### Phase 3: Atmacite Refiners, purveyors, and teleportation

1. Implement Atmacite Refiner menus for:
   - abyssite examination/upgrades,
   - atmacite equip slots unlocked by emergence periapts,
   - atmacite cruor upgrades,
   - Voidwatch teleportation for accessible zones.
2. Extend Voidwatch Purveyor shops and services beyond starter cities, including allied-notes/imperial-standing variants and pulse-cell exchanges.
3. Add teleport cost handling and destination validation by time period and stratum tier.

### Phase 4: Planar Rifts, battle lifecycle, and pyxides

1. Implement Planar Rift scripts for tagged rifts using rift IDs mapped to zone, route, tier, NM, and pyxis IDs.
2. Validate initiator progression and participating-player reward eligibility.
3. Accept phase displacers and ascent items before spawning.
4. Spawn the correct NM and attach battle state to the rift, NM, and eligible players.
5. Implement Riftworn Pyxis scripts for per-player reward retrieval and cleanup.
6. Add tests around initiation requirements, voidstone spending, and pyxis eligibility.

### Phase 5: Alignment, staggers, temporary items, and rewards

1. Implement spectral alignment state with retail color meanings and caps.
2. Implement weakness/proc candidate generation, stagger detection, messages, and synchronic blitz.
3. Implement temporary item grants/restoration from adaptability/readiness periapts.
4. Implement reward pools with red/blue quality/quantity scaling, green/yellow cruor/EXP scaling, white key-item chances, and Treasure Hunter/ascent item modifiers.
5. Add campaign setting hooks for Voidwatch campaigns that max alignments where desired by server operators.

### Phase 6: Quests and route progression

1. Add all 30 Voidwatch quest scripts in the appropriate quest regions.
2. Connect quest progression to route unlocks, NPC events, emblems, gil rewards, key items, and titles.
3. Gate Jeuno initiation behind completed starter-city paths and `Guardian of the Void` / `Drafted by the Duchy` progression.
4. Gate Provenance access behind late Jeuno-line progress.
5. Add migration-safe char variables for quest and per-NM credit flags.

### Phase 7: Provenance finale

1. Implement Walk of Echoes access objects and Provenance entry/exit flow.
2. Implement the three Radiance encounters with Kupofried's corundum entry costs, 30-minute timers, and 18-member limits.
3. Award Beguiling, Maddening, and Seductive petrifacts.
4. Implement Provenance Watcher access requiring all three petrifacts.
5. Complete finale quests through `Ad Infinitum`, including rewards, titles, and final state cleanup.

### Phase 8: NM mechanics, drops, and validation

1. Audit every Voidwatch NM pool/group/spawn against the BG Wiki route tables and retail battlefield behavior.
2. Implement missing mob scripts, immunities, skill lists, spell lists, families, draw-in, aura mechanics, and special win/loss conditions.
3. Populate and validate drop tables, pulse cell exchange paths, Heavy Metal Plate/pouch rewards, Riftcinder/Riftdross, crafting materials, atmacites, and periapts.
4. Run end-to-end GM test scripts for every route and all Provenance fights.

## Operator toggle method

Voidwatch should be toggled from `settings/default/main.lua` or an overriding settings file:

```lua
-- Hide content-tagged SQL rows when disabled.
RESTRICT_CONTENT = 1,

-- Enable or disable currently implemented Voidwatch scripts and tagged content.
ENABLE_VOIDWATCH = 1, -- set to 0 to disable
```

Current expected behavior:

- `ENABLE_VOIDWATCH = 1`: implemented Voidwatch scripts are enabled. Today this means the shared module, starter-city officers, starter abyssite grants, basic stored voidstone accrual/withdrawal, one-pouch Voiddust trades, starter-city refiner abyssite examination/upgrades, limited starter-route refiner teleport validation/cost handling, and starter-city purveyor purchases for cells, Voiddust, and phase displacers, and starter-nation tier-1 rift initiation/completion with placeholder pyxis eligibility. Quests, full rewards, full atmacite services, full teleport menus, non-starter purveyor variants, pulse-cell exchanges, and Provenance access are not yet implemented.
- `ENABLE_VOIDWATCH = 0`: implemented Voidwatch scripts return disabled-content messages and do not award starter abyssites, issue stones, accept Voiddust, sell purveyor supplies, teleport players, or progress current Voidwatch state.
- `RESTRICT_CONTENT = 1` plus `ENABLE_VOIDWATCH = 0`: content-tagged `VOIDWATCH` NPC/object rows are not loaded, hiding Planar Rifts, Riftworn Pyxides, officers, refiners, purveyors, and related static content.
- `RESTRICT_CONTENT = 0` plus `ENABLE_VOIDWATCH = 0`: static NPC/object rows may still appear, so all scripts must still check `ENABLE_VOIDWATCH` at runtime.

## Feasibility of completing Voidwatch in one PR

Completing retail-accurate Voidwatch is not feasible as a single small PR. The codebase now has the shared module, starter officers, starter abyssite grants, basic voidstone/Voiddust handling, starter refiner and purveyor services, and all three starter-nation tier-1 Planar Rift slices, but still lacks the core systems for full pyxis rewards, spectral alignment, atmacite/periapt battle effects, all 30 quest scripts, route unlocks, and Provenance finale access.

Future PRs should keep landing small vertical slices that are player-visible, runtime-guarded by `ENABLE_VOIDWATCH`, and covered by focused Lua tests.
